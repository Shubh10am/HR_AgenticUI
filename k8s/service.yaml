apiVersion: v1
kind: Service
metadata:
  name: hr-streamline-ai-service
spec:
  selector:
    app: hr-streamline-ai
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
